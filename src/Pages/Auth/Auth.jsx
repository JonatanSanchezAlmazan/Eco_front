import './Auth.css';
import FormAuth from '../../Components/FormAuth/FormAuth';

const Auth = () => {
  return (
    <main>
      <section className="content">{<FormAuth />}</section>
    </main>
  );
};

export default Auth;
